using System.Collections;
using System.Collections.Generic;
using System.Reflection;
using UnityEditor;
using UnityEngine;

/*[CustomEditor(typeof(TileTracker))]
public class TileTrackerEditor : Editor
{
    private int x;
    private int y;
    private TileTracker.SquareType type;

    public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();

        if (GUILayout.Button("Change Square"))
        {
            (target as TileTracker)?.EditorChangeSquareType(x, y, type);
        }

    }
}*/